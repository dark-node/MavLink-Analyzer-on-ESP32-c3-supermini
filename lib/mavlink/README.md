# MAVLink dialect (analyzer) — public repository

| Path | In this repo |
|------|----------------|
| `analyzer.xml` | **Published** — list of parsed message IDs |
| `generated/` | **Not published** — C headers from mavgen |
| `crc_common_table.h` | **Not published** — 295-entry CRC lookup table |

Implementation modules (`mavlink_crc.cpp`, `mavlink_tap.cpp`, `mavlink_format.cpp`)
are **stubs** in `src/`. See [docs/PUBLIC_CODE_MANIFEST.md](../../docs/PUBLIC_CODE_MANIFEST.md).
