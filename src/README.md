# Source code (partial)

| Status | Files |
|--------|--------|
| **Full source** | `app_state.cpp`, `log_ring.cpp`, `debug_serial.cpp` |
| **API only** | matching headers in `include/` |
| **Stub only** | all other `src/*.cpp`, `web_page.h` |

See [docs/PUBLIC_CODE_MANIFEST.md](../docs/PUBLIC_CODE_MANIFEST.md).

**Firmware cannot be built from this repository.**
